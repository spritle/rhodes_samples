<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @themepage.name %></h1>
    <a href="<%= url_for :action => :show, :id => @themepage.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @themepage.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @themepage.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="themepage[name]" class="fieldLabel">Name</label>
            <input type="text" id="themepage[name]" name="themepage[name]" value="<%= @themepage.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
