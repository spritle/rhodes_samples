<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @overview_page.name %></h1>
    <a href="<%= url_for :action => :show, :id => @overview_page.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @overview_page.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @overview_page.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="overview_page[name]" class="fieldLabel">Name</label>
            <input type="text" id="overview_page[name]" name="overview_page[name]" value="<%= @overview_page.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
